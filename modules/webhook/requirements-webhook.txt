# Module Webhook Dependencies
# Cài đặt: pip install -r requirements-webhook.txt

# HTTP / Async
aiohttp>=3.9.0           # Async HTTP client - gửi Discord/Slack webhook
aiofiles>=23.2.0         # Async file I/O - đọc ảnh screenshot
httpx>=0.27.0            # HTTP client sync/async - health check services

# Browser Automation (QA Sniper)
playwright>=1.42.0       # Chromium headless - chụp ảnh giao diện
# Sau khi pip install, chạy: playwright install chromium

# Web Framework (core_api integration)
fastapi>=0.110.0
uvicorn[standard]>=0.28.0

# Data Validation
pydantic>=2.0.0

# Background Tasks / Scheduling (tùy chọn)
apscheduler>=3.10.0      # Heartbeat scheduler nâng cao

# Environment
python-dotenv>=1.0.0     # Load .env file
